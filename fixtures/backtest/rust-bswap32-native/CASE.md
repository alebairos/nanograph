# Rust lang-pack native bswap32 backtest (G77)

Native no_std Rust involution specimen (`rust_native_bswap32.rs`) minted via `mint-one-rust.sh`. rev2 uses `--cfg evil_bswap` (rotl8). Witness on reject: `x=1` (same as G24 C evil).

Mint:

```
./scripts/mint-lang-pack-bswap32-backtest.sh rust fixtures/backtest/rust-bswap32-native
```
