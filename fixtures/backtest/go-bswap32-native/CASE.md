# Go lang-pack native bswap32 backtest (G77)

Native Go involution specimen (`go_native_bswap32.go`) and evil twin (`go_native_bswap32_evil.go`, rotl8) minted via `mint-one-go.sh`. Witness on reject: `x=1`.

Mint:

```
./scripts/mint-lang-pack-bswap32-backtest.sh go fixtures/backtest/go-bswap32-native
```
