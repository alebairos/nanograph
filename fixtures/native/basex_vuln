#!/usr/bin/env bash
set -euo pipefail
DIR="$(cd "$(dirname "$0")/base-x-vuln" && pwd)"
if [[ ! -d "$DIR/node_modules/base-x" ]]; then
  echo "basex_vuln: missing deps; run npm install in fixtures/native/base-x-vuln" >&2
  exit 2
fi
exec node "$DIR/../basex_codec.js" "$DIR" "$@"
