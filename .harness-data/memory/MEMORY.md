# Memory layers (NanoGraph)

| Layer | Source | Notes |
| --- | --- | --- |
| Contract | `summary.md`, `items.yaml`, `loop_state.json` | Git-tracked; no secrets |
| Episodic | — | Not used (no BrainDB) |

Write significant decisions to `items.yaml`. Refresh `summary.md` when milestone or direction changes.

Types: `decision`, `error`, `outcome`, `preference`, `context`. Include `milestone` when scoped.
