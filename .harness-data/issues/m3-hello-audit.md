## Problem

Hello P1–P3 are proven. P4 audit is the remaining layer on the canonical hello proof ladder.

## Milestone

Bound milestone: M3

## Acceptance

- [x] `tools/nano-probe/` with `audit-log` subcommand
- [x] `fixtures/hello.audit-log.golden` for genesis hello
- [x] `scripts/check-probe-audit-log.sh` passes
- [x] `check-hello-proof.sh` P4 OK (no skip)

## Verification

- [x] `./scripts/check-probe-audit-log.sh`
- [x] `./scripts/check-hello-proof.sh`

## Nano goal

G2 from [`docs/specs/NANO-GOALS.md`](../../docs/specs/NANO-GOALS.md).
